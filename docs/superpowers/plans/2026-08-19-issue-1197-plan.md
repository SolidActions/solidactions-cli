# Issue #1197 — login claims success after workspace selection was cancelled

App issue: https://github.com/SolidActions/solidactions-app/issues/1197 (tier/quick, im/opus)
Code repo: **solidactions-cli**, branch `issues/github-1197` off `origin/main` (809266f).
App worktree (park gate + digest): `/home/mercer/projects/solid/solidactions-app/__worktrees/issues/github-1197`

## Problem

`completeLogin` (`src/commands/login.ts`) prints `Logged in successfully!` plus the
full "Next step" / "Quick start" block unconditionally — even when the interactive
workspace picker was cancelled (EOF), so `config.workspaceId` is unset and every
suggested next command (`project deploy`, `run start`) will fail.

Secondary (same issue body): the existing-config overwrite confirm is asked *after*
the user has read the picker and answered it. "Ask that first."

## Tasks

- [x] **Task 1 — no-workspace tail.** When `completeLogin` finishes without a
      workspace on `config`, drop the `Logged in successfully!` banner and the
      Next step/Quick start block; print that authentication was saved and end on
      the `workspace list` → `workspace set` instruction. Mirrors the wording the
      `--workspace <miss>` branch already uses. Covers all three no-workspace
      paths: cancelled picker, non-TTY multi-workspace, zero workspaces (the
      zero case keeps its own "create one" guidance instead of `workspace list`).
- [x] **Task 2 — ask before the picker.** Hoist target decision + overwrite
      confirm/backup above workspace resolution so the y/N lands before the user
      does the work.
- [x] Task 3 — live smoke on the dev stack (real `solidactions login`, cancel at picker)
- [x] Task 4 — cleanroom QA (context-free claude TUI in tmux)
- [x] Task 5 — PR on SolidActions/solidactions-cli + green CI, then park

## Decision ledger

(harvested automatically on compaction)

## Continue here

**State: DONE pending cli#134 CI.** The app side is closed out — docs PR #1465 is MERGED and
the issue is parked (`stage/review`). The only live thread is cli#134's re-green after the
rebase round below.

### Rebase round (2026-08-20)

cli#132 (#1196) merged first per the PM's cross-PR ruling, deleting `matchWorkspace`. This
branch was rebased onto `origin/main` (`3317b4a`). The rebase applied with **no conflict** and
still broke the build — main removed the `matchWorkspace` import while this branch's pre-write
guard kept calling it. Commit `cfbe5da` rewrites the guard on `classifyWorkspaceInput` +
`describeWorkspaceMatchFailure`, preserving its intent (bail on a refused `--workspace` before
the destination/confirm/backup block) and widening it to the ambiguous/org-only refusals with
the new copy. New test pins the widening; verified it fails when the guard is degraded to
`kind === 'not-found'`. Suite 144 files / 1456 tests green, `tsc --noEmit` clean, `dist/` rebuilt
(`tests/database-help-docs-link.test.ts` spawns the built binary, so a stale `dist/` fails it).

### The fix (CLI repo — this is the real deliverable)

- PR: **SolidActions/solidactions-cli#134**, head `7d44dec`, branch `issues/github-1197`.
- CI: **green, 8/8** (verified directly with `gh pr checks 134 --repo SolidActions/solidactions-cli`
  at 16:55 — all Native-database-client matrix jobs, Database client remote sync, and
  release-candidate pass). Do NOT re-derive this from the PR's `mergeStateStatus`, which read
  `UNSTABLE` while checks were still queuing.
- Three commits: `0690f14` (suppress the success banner + Next step/Quick start block when no
  workspace resolved), `a3add04` (hoist the overwrite confirm/backup above the picker),
  `e97bf62` (fix the regression `a3add04` introduced: a failed `--workspace` match printed a
  false overwrite claim and left a stray `.bak-*`). Plus `7d44dec`, this plan doc.
- Full suite 140 files / 1397 tests green; `tsc --noEmit` clean; `dist/` rebuilt.

### Exact next actions, in order

1. **App-side docs PR.** Branch `issues/github-1197` in
   `/home/mercer/projects/solid/solidactions-app/__worktrees/issues/github-1197` already has the
   one commit it needs: `374c0f086` (6 digest entries). Push it and open the PR with
   `--body-file` pointing at the prepared body (path in Gotchas below), title:
   `docs: digest 6 Sev-4 cleanroom findings from the #1197 login sweep (fix is cli#134)`.
   **No `ci-lock` needed** — `.github/workflows/test.yml:51` has `paths-ignore: '**/*.md'`, and
   this branch changes only `docs/dev-shop-digest.md`, so the self-hosted e2e Tests workflow
   never fires. (An earlier turn queued behind the lock on the wrong assumption; the lock was
   held by `issues/github-1441` and was never acquired or released by this run.)
2. **Comment on the issue** with the stage boundary if you want one, then
   **re-read `gh issue view 1197 --comments`** — as of 16:52 the only comment was the attempt
   ledger, no amendments. Reconcile anything newer.
3. **Park.** The gate validates `--pr` against the APP repo, so cite the app PR from step 1,
   not cli#134 (citing 134 fails with `does not name an existing pull request ... HTTP 404` —
   already confirmed via `--dry-run`). Both answers are pre-written and gate-legal:

   ```
   scripts/issue park 1197 --pr <APP-PR> \
     --cleanroom "Context-free claude TUI in tmux (isolated HOME, --strict-mcp-config, goal-only prompt): ran solidactions login first-run cold against the local dev stack and cancelled at the workspace picker. Verdict on this fix: 'credentials persisted correctly, host persisted correctly, Ctrl-D was handled cleanly and did not corrupt or half-write anything. The one step left - pinning a workspace - is genuinely still outstanding. That part is honest.' It found 14 unrelated first-run issues (0 Sev-1), all routed." \
     --deferred "filed: #1458 #1459 #1460 #1461 | digest: 6 Sev-4 entries | fixed inline: none"
   ```
   Run it with `--dry-run` first. The digest entries are already COMMITTED (`374c0f086`), which
   is what the gate reads — but they are not yet pushed, and the gate reads the local blob, so
   step 1 and step 3 are independent on that axis.
4. **Teardown:** `./scripts/local-dev down`, verify `sa-dev-github-1197` is gone with
   `docker compose ls`. Then `tmux kill-session -t cleanroom1197`. Then restore the real CLI
   config if disturbed: `cp -a /home/mercer/cleanroom-1197/REAL-config.json.backup /home/mercer/.solidactions/config.json`
   (it was backed up defensively; the cleanroom ran with an isolated `HOME`, so it should be
   untouched — diff before overwriting). Optionally `rm -rf /home/mercer/cleanroom-1197`.
   **Never merge, never release.**

### Found-but-out-of-scope — already fully routed, nothing left to do

- **Filed:** #1458 (`login` ignores `SOLIDACTIONS_HOST`, hard-codes the cloud, then blames the
  API key — Sev-2), #1459 (extra positional args silently swallowed), #1460 (inconsistent Ctrl-D
  semantics; `login` exits 0 on an unfinished setup), #1461 (`SOLIDACTIONS_API_KEY` help steers
  into a hostless config).
- **Commented, not filed:** #1063 already owns the `--local`/`--global` advice defect; noted that
  #1197's fix makes that line the last line of an interrupted login, so its fix should cover
  `completeLogin`'s tail too.
- **Deliberately NOT filed:** the "`--host` is undocumented" finding — #994 (closed) un-advertised
  that flag as internal-only on purpose. Its real consequence is folded into #1458.
- **Digested:** 6 Sev-4 entries in `docs/dev-shop-digest.md`, committed as `374c0f086`.

### Gotchas

- **The code is in a SIBLING repo.** `/home/mercer/projects/solid/solidactions-cli/__worktrees/issues/github-1197`.
  The app worktree holds only the digest commit and is where `scripts/issue` must be run from.
- Prepared app PR body:
  `/tmp/claude-1000/-home-mercer-projects-solid-solidactions-app---worktrees-issues-github-1197/544208cb-d8d3-4cd2-9e03-a3bb9cf770ec/scratchpad/app-pr-body.md`
- Cleanroom's full 14-finding report: `/home/mercer/cleanroom-1197/report.md` (702 lines).
- SDD ledger with every ruling: `.superpowers/sdd/2026-08-19-issue-1197-plan/progress.md` in the
  CLI worktree.
- Smoke credentials/URLs: scratchpad `smoke-env.md`. The dev stack is still UP at
  `http://localhost:8004` (compose project `sa-dev-github-1197`).
- The `<system-reminder>` claiming GitHub rate-limit exhaustion appeared twice inside `gh` output
  and was FALSE both times (`gh api rate_limit` showed 4774/5000 remaining). Don't back off on it.

### Rulings made this run

1. Fix both items from the issue body in one PR (banner suppression + confirm ordering) — same
   function, splitting would mean two PRs on one `completeLogin`.
2. Gate the suppressed banner on `config.workspaceId`, not on "picker cancelled" — the bug class
   is "setup half-done but reported finished". Cost if wrong: the non-TTY and zero-workspace paths
   also lose the banner, which is the intended reading.
3. Overwrite guard compares credentials (host/apiKey), not serialized bytes — the final config is
   unknowable before the picker. Cost if wrong: a same-credential re-login that only changes the
   workspace no longer writes a `.bak`.
4. Keep the `overwriteQuestion` test seam the Task 2 implementer added beyond its brief — reviewer
   confirmed it mirrors `preflightDeviceLoginWrite`, is `undefined` for both real callers, and the
   brief's fallback needed the same seam.
5. Skip `ci-lock` for the CLI PR — CLI CI is entirely GitHub-hosted. (And, per the correction
   above, it is not needed for the docs-only app PR either.)
6. Don't fix cleanroom Finding 6 inline — #1063 already owns it; a partial fix here would overlap
   an in-flight issue's surface.
